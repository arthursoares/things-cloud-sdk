version: "2"

linters:
  default: none
  enable:
    - govet
    - ineffassign
  exclusions:
    generated: disable

formatters:
  enable:
    - gofmt
  settings:
    gofmt:
      simplify: false
  exclusions:
    generated: disable
