version: "2"

linters:
  default: none
  enable:
    - copyloopvar
    - errorlint
    - govet
    - ineffassign
    - misspell
    - staticcheck
    - unused
  exclusions:
    generated: disable

formatters:
  enable:
    - gofmt
  settings:
    gofmt:
      simplify: false
  exclusions:
    generated: disable
