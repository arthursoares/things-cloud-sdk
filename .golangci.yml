version: "2"

linters:
  default: standard
  enable:
    - bodyclose
    - copyloopvar
    - errorlint
    - misspell
    - rowserrcheck
  exclusions:
    presets:
      - std-error-handling

formatters:
  enable:
    - gofmt
