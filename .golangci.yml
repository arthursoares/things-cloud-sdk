version: "2"

linters:
  default: none
  enable:
    - bodyclose
    - copyloopvar
    - errorlint
    - govet
    - ineffassign
    - misspell
    - rowserrcheck
    - staticcheck
    - unused
  exclusions:
    generated: disable

formatters:
  enable:
    - gofmt
  settings:
    gofmt:
      simplify: false
  exclusions:
    generated: disable
